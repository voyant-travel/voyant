{
  "$comment": [
    "Public API bundles that are deliberately secret-key-only (voyant#4625 §1).",
    "",
    "A bundle that declares neither `publishable` nor `guardedIntake` is",
    "secret-only, which is the correct fail-closed default — and is also exactly",
    "what an unclassified bundle looks like. Naming it here is what tells the two",
    "apart, so the entry is the audit trail: say WHY a browser credential must",
    "not reach it.",
    "",
    "Removing an entry without adding a posture fails the checker, and so does",
    "leaving an entry for a bundle that has since declared one."
  ],
  "bundles": {}
}
