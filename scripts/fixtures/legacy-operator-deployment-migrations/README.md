# Legacy Operator deployment migrations

This frozen SQL history exists only to exercise upgrades from databases that
applied the retired `deployment` migration source. Runtime and starter code must
not load it. Current link tables and views are owned by package migrations.
