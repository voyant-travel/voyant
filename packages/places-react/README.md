# @voyantjs/places-react

React hooks, query helpers, and reusable UI for shared places.

This package is the forward React package name for the existing facilities UI.
It re-exports the compatible `@voyantjs/facilities-react` implementation while
adding place-first names such as `PlaceCombobox`, `PlaceBadge`, `usePlaces`, and
`placesQueryKeys`.
