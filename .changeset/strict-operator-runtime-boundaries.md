---
"@voyant-travel/operator-runtime": patch
---

Validate required auth secrets when loading an Operator project, adapt the
generic Node host contracts to auth, webhook, and scheduled workflow runtimes,
and exclude test sources from the published package build.
