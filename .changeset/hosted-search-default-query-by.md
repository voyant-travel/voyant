---
"@voyantjs/catalog": patch
---

Expose policy-derived default Typesense `query_by` helpers and collection metadata so hosted search can apply curated searchable fields when callers omit `query_by`.
