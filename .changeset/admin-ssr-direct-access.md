---
"@voyantjs/admin": patch
---

Expose stable dashboard aggregate query keys and the default dashboard date window so server-rendered admin apps can preload dashboard data through direct service access while keeping client query hydration aligned.
