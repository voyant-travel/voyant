---
"@voyant-travel/workflows-orchestrator": minor
---

Expose the in-memory orchestration driver via a dedicated `./in-memory` export so deployments (e.g. the operator starter) can run workflows locally without an external orchestrator.
