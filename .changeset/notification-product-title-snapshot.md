---
"@voyant-travel/notifications": patch
---

Resolve `product.title` in notification templates from the booking item's catalog
snapshot instead of its unit/option label, and stop overwriting a caller-supplied
`product`.
