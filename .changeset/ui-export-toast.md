---
"@voyant-travel/ui": minor
---

Re-export sonner's `toast` from the components barrel, so consumers fire toasts via `@voyant-travel/ui` (alongside the already-exported `Toaster`) instead of importing `sonner` directly.
