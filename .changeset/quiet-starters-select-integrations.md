---
"@voyant-travel/framework": patch
---

Declare that the standard Node starter has no default plugins so external integrations such as SmartBill are admitted only when a project selects them explicitly.
