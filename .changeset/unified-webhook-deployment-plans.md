---
"@voyant-travel/framework": minor
---

Compile selected graph webhook declarations into generated deployment/runtime plans, validate typed inbound API and outbound event references, enforce inbound route posture, and expose outbound webhook event eligibility at runtime.
